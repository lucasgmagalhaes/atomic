[package]
name = "import"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }

[dev-dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
