//! Thin Rust bindings over CEF's C API — see `spec/architecture/cef-integration.md`.
//! Nothing here yet: this crate exists so the workspace can start
//! resolving/linking against `cef` (the `cef-rs` project's safe wrapper)
//! before any real profile-process logic is built on top of it.
